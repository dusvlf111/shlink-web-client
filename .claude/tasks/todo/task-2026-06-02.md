# Task Index — 2026-06-02 (PRD-0602)

> PRD: `.claude/tasks/todo/prd-0602.md`
> 원본 메모: `.claude/tasks/0602.md`
> 생성: task-maker (에이전트 팀 구성 반영)

## Push 목록

| Push | 파일 | 범위 | 우선순위 | 팀 구성 | 상태 |
|------|------|------|:---:|---------|:---:|
| 1 | `tasks-prd-0602-push1.md` | UTM 벌크 전체해제 + 템플릿 수정→저장 미적용 | P0 | coordinator→dev+test | 🔲 |
| 2 | `tasks-prd-0602-push2.md` | 서버 추가 PocketBase 일원화(로컬 fallback 제거) | P0 | dev+test | 🔲 |
| 3 | `tasks-prd-0602-push3.md` | 방문 봇 기본 제외(`excludeBots`) | P0 | dev+test | 🔲 |
| 4 | `tasks-prd-0602-push4.md` | UTM 빌더 다중 링크 입력 + 역할 부제 | P1 | coordinator→dev+test | 🔲 |
| 5 | `tasks-prd-0602-push5.md` | 헤더 PocketBase 링크(admin) + 설정>방문 한글화 | P1 | dev+test | 🔲 |

**권장 실행 순서:** P0 먼저 (1 → 2 → 3) → P1 (4 → 5). 각 Push는 독립 배포 가능.

## 범위 밖 (별도 추적)
- **§3-B 해외 트래픽 수집 차단**: Coolify/리버스 프록시 GeoIP 또는 Shlink 서버 설정. 인프라 접근 권한 필요 — 운영 티켓.

## 에이전트 역할 (`.claude/roles/`)
| 역할 | 담당 | 모델 |
|------|------|------|
| coordinator | 작업 분배·조정·진행 관리 (Task로 dev/test 호출) | inherit |
| developer | 구현·커밋 (Skill 없음 — task 파일의 규칙 요약 참조) | inherit |
| tester | 타입·린트·테스트·빌드 검증, 간단 UI 수정 | haiku |

## ⚠️ 이 레포 구조 주의 (역할문서와 다름)
- 이 프로젝트는 **기능 평면 구조**(`src/utm`, `src/servers`, `src/settings`, `src/common`, `src/lib`, `src/i18n` …).
- role 문서(developer.md)의 **FSD 계층(app/pages/widgets/features/entities/shared)은 이 레포에 적용 안 됨.**
- import는 기존 관례인 **상대경로** 사용(`@/` alias 아님).
- i18n: 커스텀 UI 문자열 = `src/i18n/locales/{ko,en}.ts`. 외부 패키지(`@shlinkio/shlink-web-component`) 문자열 = `scripts/patch-shlink-i18n.mjs` 빌드 패치.

## 공통 개발 규칙 (스킬 요약)

### code-quality (4기준)
1. 가독성 — 명확한 네이밍, 한 번에 보는 맥락 최소화
2. 예측 가능성 — 이름·반환만으로 동작 예측, **에러를 삼키지 말 것**
3. 응집도 — 함께 바뀌는 코드는 한 곳에
4. 결합도 — 수정 영향 범위 최소화

### vercel-react-best-practices (이번 작업 관련)
- effect 의존성 최소화 — 불필요 의존성 제거(Push1: `selectedIds.length` 제거)
- 파생 상태는 `useMemo`로(Push4: 결과 목록), 중복 상태 보관 금지
- 함수형 setState, 정적 값/JSX 호이스팅, 불필요 리렌더 방지

### 테스트 (vitest)
- 단위/컴포넌트 테스트는 `test/<feature>/...`에 위치. 실행: `npx vitest run test/<path>`
- 공통 검증: `npx tsc --noEmit` → `npx vitest run` → `npx eslint <path>` → 필요 시 `npm run build`

## 참조 문서/이미지 전체 경로
- PRD: `.claude/tasks/todo/prd-0602.md`
- 역할: `.claude/roles/{coordinator,developer,tester}.md`
- 스킬: `.claude/skills/{code-quality,vercel-react-best-practices,folder-structure}/SKILL.md`
- (이미지 없음 — 본 PRD는 텍스트 기반)

## 핵심 수정 파일 맵
| 영역 | 파일 |
|------|------|
| UTM 데이터 훅 | `src/utm/useUtmData.ts` |
| UTM 벌크 | `src/utm/UtmBulkBuilderPage.tsx` |
| UTM 템플릿/태그 관리 | `src/utm/UtmTemplateManager.tsx`, `src/utm/UtmTagManager.tsx` |
| UTM 빌더 | `src/utm/UtmBuilderPage.tsx` |
| 서버 생성 | `src/servers/CreateServer.tsx` |
| 설정 기본값 | `src/settings/reducers/settings.ts` |
| 헤더 | `src/common/MainHeader.tsx` |
| i18n(커스텀) | `src/i18n/locales/{ko,en}.ts` |
| i18n(외부 패키지) | `scripts/patch-shlink-i18n.mjs` |
